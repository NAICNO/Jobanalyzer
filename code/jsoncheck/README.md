This is a simple syntax checker for JSON data.  Given a filename it tries to unmarshal the file as
json.

This could probably be replaced by the common `jq` utility (unknown to the author when jsoncheck was
written).
